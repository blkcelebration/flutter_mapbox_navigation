import 'package:flutter/material.dart';

class SharedStyles {
  static final List<BoxShadow> shadow = [
    BoxShadow(color: Colors.grey.withOpacity(0.3), spreadRadius: 1, blurRadius: 4, offset: const Offset(0, 0)),
  ];
}
